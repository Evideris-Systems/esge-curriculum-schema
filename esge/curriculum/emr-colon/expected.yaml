# Expected coverage manifest for Tate EMR 2023 (DOI 10.1055/a-2077-0497).
#
# This file is the HUMAN'S PROMISE about source coverage. It is the input
# to scripts/completeness-vs-expected.py which fails `make check` if the
# encoded JSON diverges from this declaration without a stated reason.
#
# Updating this file means re-reading the source paper and confirming the
# expected counts. The file IS the source-fidelity audit; it leaves an
# artefact.
#
# A rec entry can declare:
#   subItems: <int>             — exact expected count
#   subItems: known-incomplete  — extraction pending; rec carries _meta.knownIncomplete
#
# Any rec NOT listed here makes the gate fail too — forces the human to
# audit every rec before merge.

source_doi: 10.1055/a-2077-0497
reviewedBy: human:david
reviewedAt: 2026-06-28
forCurriculum: cur-emr-colon-2023

mainStatements:    11
sections:          8
qis:               18
gpatItems:         21
references:        150

# Counts reflect actual VERBATIM source coverage as it stands today. Counts
# that say known-incomplete are tracked in task #20 for verbatim extraction.
recommendations:
  1:  { subItems: 0, reason: "competencies listed inline in statement text, not as (i)(ii) items" }
  2:  { subItems: 3 }
  3:  { subItems: 2 }
  4:  { subItems: 7 }
  5:  { subItems: 15 }
  6:  { subItems: 9 }
  7:  { subItems: 9 }
  8:  { subItems: 7 }
  9:  { subItems: 17 }
  10: { subItems: 5 }
  11: { subItems: 4 }
  12: { subItems: 3 }
  13: { subItems: 11 }
  14: { subItems: 9 }
  15: { subItems: 13 }
  16: { subItems: 7 }
  17: { subItems: 6 }
  18: { subItems: 6 }
  19: { subItems: 17 }
  20: { subItems: 12 }
  21: { subItems: 7 }
  22: { subItems: 10 }
  23: { subItems: 1, reason: "single composite item; the STSC technique is a numbered (i) with embedded bullet sub-points in source — encoded as one item per source structure" }
  24: { subItems: 1, reason: "source lists one numbered (i) item (cold avulsion + soft coagulation) plus a Comment paragraph mentioning alternatives (hot avulsion, APC, ESD, FTR) outside scope" }
  25: { subItems: 18 }
  26: { subItems: 6 }
  27: { subItems: 6 }
  28: { subItems: 10 }
  29: { subItems: 12 }
  30: { subItems: 12 }
  31: { subItems: 9 }
  32: { subItems: 5 }
  33: { subItems: 8 }
  35: { subItems: 7 }
  36: { subItems: 6 }
  37: { subItems: 4, reason: "encoded current items (i)-(iv); source items (v) and (vi) explicitly labelled 'potential future points' in §7.1 — excluded from canonical encoding" }
  38: { subItems: 0, reason: "points to Table 6 (separate QI artifacts); no enumerated sub-items in source" }
